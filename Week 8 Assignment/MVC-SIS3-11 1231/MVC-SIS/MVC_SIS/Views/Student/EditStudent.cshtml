@*@model Exercises.Models.Data.Student*@

@model Exercises.Models.ViewModels.StudentVM

@{
    ViewBag.Title = "EditStudent";
}

<div class="row">
    <div class="col-xs-12">
        <h3>Edit Student</h3>
        @Html.ActionLink("Return to List", "List", "Student")
    </div>
</div>
<div class="row">
    <div class="col-xs-12">
        @using (Html.BeginForm())
        {
        @Html.HiddenFor(m => m.Student.StudentId)
        <div class="form-group">

            <label></label>
            @Html.TextBoxFor(m => m.Student.FirstName, new { @class = "form-control", placeholder = "Student Last Name" })
            @Html.TextBoxFor(m => m.Student.LastName, new { @class = "form-control", placeholder = "Student Last Name" })<br />


            @*@{string crntMajor = "Select a major";}*@
            @{string crntMajor = this.Html.ViewData.Model.Student.Major.MajorName.ToString();}

            @Html.DropDownListFor(m => m.Student.Major.MajorId, Model.MajorItems, @crntMajor, new { @class = "form-control" })

            @*@Html.DropDownListFor(m => m.Student.Major.MajorId, Model.MajorItems, "-Choose One-", new { @class = "form-control" })*@

            @*@Html.TextBoxFor(m => m.Student.Major.MajorName, new { @class = "form-control", placeholder = "m.Student.Major.MajorName" })*@
            @Html.TextBoxFor(m => m.Student.GPA, new { @class = "form-control", placeholder = "Student GPA" })

            <h4>Courses</h4>
            <div class="form-group">
                @Html.ListBoxFor(m => m.SelectedCourseIds, Model.CourseItems, new { @class = "form-control" })
            </div>

            @*/// The text below if for editing student address///*@
            <h4>Address</h4>

            @{ var AddressPlaceHolder = this.Html.ViewData.Model.Student.Address.Street1.ToString();}

            @Html.TextBoxFor(m => m.Student.Address.Street1, new { @class = "form-control", placeholder = "Address Street1" })
            @Html.TextBoxFor(m => m.Student.Address.Street2, new { @class = "form-control", placeholder = "Address Street2" })
            @Html.TextBoxFor(m => m.Student.Address.City, new { @class = "form-control", placeholder = "City" })
            <label>State:</label>
            @*@Html.DropDownListFor(m => m.Student.Major.MajorId, Model.MajorItems, "-Choose One-", new { @class = "form-control" })*@
            @Html.DropDownListFor(m => m.Student.Address.State, Model.StateItems, "-Choose One-", new { @class = "form-control" })

            @Html.TextBoxFor(m => m.Student.Address.PostalCode, new { @class = "form-control", placeholder = "m.Student.Address.PostalCode" })<br />

        </div>
        <button type="submit" class="btn btn-sm btn-primary">Save</button>
        }
    </div>
</div>

